#include <stdio.h>
int main(void)
{
	int n;
	int i;
	double t;
	double sum = 0;
	printf("请输入一个正整数："); scanf_s("%d", &n);
	for (i = 1; i < n+1; i++) {
	    t = 1.0/i;/*求前n项之和，避免整型溢出的问题，新定义一个浮点型t*/
		sum = sum + t;
	}
	printf("%f", sum);
	return(0);
}
