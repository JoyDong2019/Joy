#include<stdio.h>
#include<math.h>
int main()
{
	int i, a, n, s1 = 0, s = 0;
	int t;
	scanf_s("%d%d", &a, &n);
	for (i = 1; i <= n; i++) {
		t = i - 1;
		s1 += pow(10, t ) * a;/*pow函数的应用*/
		s += s1;
	}
	printf("s = %d", s);
	return 0;
}
