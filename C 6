计算序列 1 - 1/4 + 1/7 - 1/10 + ... 的前N项之和

#include <stdio.h>
int main(void)
{
	int N;
	int count = 1;
	int i=1;
	double t;
	double sum = 0;
	printf("请输入一个正整数N："); scanf_s("%d", &N);
	do {
		if (count % 2 == 1)
			t = 1.0 / i;
		else
			t = -1.0 / i;
		sum = sum + t;
		i = i + 3;
		count++;
	} while (count < N+1);
	printf("%.3f", sum);
	return(0);
}
