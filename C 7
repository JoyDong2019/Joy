生成3的乘方表
#include <stdio.h>
#include<math.h>
int main(void)
{
	int N;
	int i = 0;
	int f;
	printf("请输入一个正整数："); scanf_s("%d", &N);
	for (i = 0; i <= N; i++) {
		f = pow(3, i);
		printf("pow(3,i)=%d\n",f);
	}
	return(0);
}
